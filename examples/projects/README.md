# Projects

Nested project folders for testing deep indentation and tree navigation.

- **alpha/** — a sample web project (HTML, CSS, notes)
- **beta/** — a deeply nested folder structure (5 levels), each level with its own `README.md`
