# HTML Samples

A small nest of HTML fixtures across `folder1/`, `folder2/`, and `folder3/` — useful for testing tree navigation *and* HTML rendering at the same time. Includes [lorem.htm](lorem.htm).
